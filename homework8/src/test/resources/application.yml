spring:
  mongodb:
    embedded:
      version: 4.4.9
  data:
    mongodb:
      database: library_test

  shell:
    interactive:
      enabled: false


mongock:
  enabled: false


logging:
  level:
    root: ERROR
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
